# GitBrowser — План редизайна

## Концепция

Стиль: **Arc Browser × GitHub × iOS glassmorphism**

- Боковая панель (sidebar) вместо верхних вкладок — как в Arc Browser
- Стеклянные (glass) эффекты: `backdrop-filter: blur()`, полупрозрачные панели
- Скруглённые углы в стиле iOS (12-16px radius)
- GitHub-палитра цветов, но с глубиной и слоями
- Плавные анимации: spring-based easing, выезжающие панели
- Минимализм: меньше границ, больше пространства и теней

---

## Архитектура UI

### Текущая структура (что есть)
```
┌──────────────────────────────────────┐
│ [tabs] [◄ ► ↻] [url bar] [buttons]  │  ← toolbar.html (74px)
├──────────────────────────────────────┤
│                                      │
│           web content                │  ← WebContentsView
│                                      │
└──────────────────────────────────────┘
```

### Новая структура (что будет)
```
┌────────┬─────────────────────────────┐
│ SIDEBAR│  [◄ ► ↻] [url bar] [⋯]    │  ← compact nav bar (44px)
│        ├─────────────────────────────┤
│ pinned │                             │
│ tabs   │                             │
│        │       web content           │
│ ─────  │                             │
│ spaces │                             │
│ tabs   │                             │
│        ├─────────────────────────────┤
│ ─────  │                             │
│ ☆ ⏱ ⬇ │                             │
│ ⚙ 🔑  │                             │
└────────┴─────────────────────────────┘
   240px          остальное
```

---

## 1. Sidebar (боковая панель) — КЛЮЧЕВОЕ ИЗМЕНЕНИЕ

### Структура sidebar
- **Верх**: логотип GitBrowser (маленький) + кнопка сворачивания
- **Pinned tabs**: закреплённые вкладки (только favicon, 32x32)
- **Spaces / группы**: разделители для организации вкладок
- **Обычные вкладки**: список с favicon + title, выделение активной
- **Низ**: быстрые кнопки (bookmarks, history, downloads, passwords, settings, extensions, AI, GitHub)

### Поведение
- Sidebar сворачивается до 48px (только иконки) по кнопке или горячей клавише
- Вкладки в sidebar — вертикальный список, drag-and-drop для перестановки
- Hover на свёрнутом sidebar — плавное выезжание (overlay, не сдвигает контент)
- Правый клик на вкладке — контекстное меню (pin, mute, close, duplicate)
- Закрытие вкладки — анимация схлопывания (height → 0 + opacity → 0)
- Новая вкладка — анимация появления снизу

### Стиль
- Фон: `rgba(22, 27, 34, 0.85)` + `backdrop-filter: blur(20px) saturate(180%)`
- Активная вкладка: подсветка слева (2px accent bar) + subtle bg
- Hover: мягкая подсветка фона
- Разделители: тонкие линии `1px solid rgba(255,255,255,0.06)`
- Скруглённые элементы: 8px radius на вкладках

---

## 2. Navigation Bar (верхняя панель) — УПРОЩЕНИЕ

### Текущее (74px, перегружено)
Tab bar + nav buttons + URL bar + 10 кнопок

### Новое (44px, минимализм)
- Только: `[◄] [►] [↻] [────── URL ──────] [☆] [⋯]`
- Кнопка `⋯` (три точки) — выпадающее меню со всеми действиями
- Bookmark star — прямо в URL bar (как в Chrome)
- URL bar: стеклянный стиль, скруглённый (12px radius)
- Lock icon внутри URL bar (слева)

### Стиль
- Фон: прозрачный, сливается с контентом
- URL bar: `background: rgba(255,255,255,0.06)`, `border: 1px solid rgba(255,255,255,0.08)`
- Focus: `box-shadow: 0 0 0 2px rgba(88,166,255,0.4)`, border accent
- Кнопки навигации: 32px, ghost style (без фона, только hover)

---

## 3. Glassmorphism — ВИЗУАЛЬНЫЙ СТИЛЬ

### CSS переменные (новые)
```css
:root {
  /* Glass */
  --glass-bg: rgba(22, 27, 34, 0.75);
  --glass-border: rgba(255, 255, 255, 0.08);
  --glass-blur: blur(20px) saturate(180%);
  --glass-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);

  /* Radius */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 20px;

  /* Transitions */
  --spring: cubic-bezier(0.34, 1.56, 0.64, 1);
  --ease-out: cubic-bezier(0.33, 1, 0.68, 1);
  --duration-fast: 150ms;
  --duration-normal: 250ms;
  --duration-slow: 400ms;
}
```

### Применение
- Sidebar: glass background
- URL bar: glass input
- Dropdown menus: glass panels
- Modals/dialogs: glass overlay + glass card
- Toast notifications: glass pill shape
- Internal pages: subtle glass cards

---

## 4. Внутренние страницы — РЕДИЗАЙН

### Общий шаблон
- Убрать `padding: 32px 48px` → использовать centered container с `max-width: 720px`
- Заголовок страницы: крупный (32px), с иконкой слева
- Карточки: glass style, 12px radius, мягкие тени
- Списки: без явных borders, разделение через spacing
- Кнопки: pill shape (20px radius), ghost/filled варианты
- Inputs: glass style, 10px radius

### newtab.html
- Убрать часы (перегружает)
- Оставить: приветствие + поисковая строка (крупная, по центру)
- Quick links: 2 ряда по 4, glass cards с favicon
- Фон: subtle gradient или solid color (настраиваемый)
- Минимализм: ничего лишнего

### settings.html
- Sidebar навигация внутри страницы (как в macOS System Preferences)
- Секции: General, Appearance, Privacy, Search, Downloads, Advanced
- Toggle switches: iOS-style (pill shape)
- Selects: custom dropdown с glass style

### bookmarks.html
- Grid layout (карточки) вместо списка
- Каждая карточка: favicon + title + URL preview
- Поиск сверху с glass input
- Папки как отдельные секции

### history.html
- Timeline layout (вертикальная линия слева)
- Группировка: Сегодня / Вчера / Ранее
- Каждая запись: favicon + title + time + URL
- Поиск + кнопка очистки

### downloads.html
- Карточки с прогресс-баром внутри
- Статус: цветной badge (зелёный/жёлтый/красный)
- Кнопки: ghost style, появляются при hover

### passwords.html
- Unlock screen: centered, минималистичный
- Список: карточки с favicon сайта + username
- Пароль скрыт, кнопка "показать"
- Генератор: slider для длины, toggles для опций

### ai.html
- Chat bubbles: скруглённые (16px), glass style
- Sidebar с историей чатов
- Input: glass textarea, кнопка отправки — accent pill

### github.html
- Profile card: glass, аватар + stats
- Tabs: Repos / Issues / PRs / Notifications / Activity
- Карточки репозиториев: glass, с language badge

### extensions.html
- Карточки расширений: glass, с toggle switch
- Install: кнопка + drag-and-drop зона
- Детали расширения: permissions badges

---

## 5. Анимации

### Sidebar
- Открытие/закрытие: `width` transition 250ms spring
- Вкладки: stagger fade-in при загрузке
- Закрытие вкладки: slide-out + collapse height
- Новая вкладка: slide-in from bottom

### Страницы
- Переход между страницами: crossfade (opacity 150ms)
- Элементы списков: stagger fade-in (50ms delay между элементами)
- Карточки: scale(0.98) → scale(1) при появлении
- Hover на карточках: subtle lift (translateY(-2px) + shadow increase)

### Модалы
- Появление: scale(0.95) + opacity(0) → scale(1) + opacity(1), 200ms spring
- Overlay: opacity fade 150ms

### Toast
- Slide-in from top-right, pill shape
- Auto-dismiss: slide-out after 3s

---

## 6. Найденные баги (исправить в процессе редизайна)

### Критические
- [x] `extensions.html`: опечатка `--fg-muоted` (кириллическая "о") → `--fg-muted` — из-за этого цвет muted текста не работает в dark theme
- [x] Extensions: `prompt()` для ввода пути — нужен нормальный file picker (Electron dialog)
- [x] Extensions: нет preload API для `openExtensions` — кнопка в toolbar не открывает страницу
- [ ] Sidebar в AI assistant: chat sessions хранятся в localStorage, не синхронизируются с Rust backend
- [x] AI chat: API ключи читаются из localStorage toolbar'а через `executeJavaScript` — хак, нужно использовать `secret.store`/`secret.get`
- [x] GitHub: при device flow login, `login` всегда передаётся как `'user'` вместо реального username
- [x] Private window: нет отдельного session/partition — cookies и данные шарятся с основным окном
- [x] Find bar: не закрывается при переключении вкладок (close-find event не отправляется)
- [x] Zoom: уровень не восстанавливается при переключении вкладок

### UI баги
- [x] Toolbar: кнопки extensions и passwords не имеют tooltip'ов
- [x] newtab: quick links favicon загружается через Google API (может быть заблокирован)
- [x] history: при пустой истории нет placeholder'а с иконкой
- [x] bookmarks: при добавлении через toolbar, title может быть пустым (race condition с page-title-updated)
- [x] downloads: toast "Download complete" не кликабельный (нельзя открыть файл из toast'а)
- [x] settings: нет кнопки "Reset to defaults"
- [x] Context menu: AI actions (Fix, Rephrase, Translate) не работают если нет API key — нет понятного сообщения

### Стилевые проблемы
- [x] Все внутренние страницы дублируют CSS переменные (copy-paste) — нужен общий файл → создан `design-tokens.css`
- [x] Нет единого design system — каждая страница стилизована отдельно → создан `components.css`
- [x] border-radius: 0 в common.css конфликтует с border-radius: 8px в HTML страницах
- [x] Нет dark/light theme transition (мгновенное переключение, без анимации)
- [x] Toolbar и внутренние страницы используют разные размеры шрифтов

---

## 7. Технические улучшения

- [x] Вынести CSS переменные в единый `design-tokens.css`, подключать во все страницы
- [x] Создать `components.css` с переиспользуемыми компонентами (buttons, inputs, cards, badges)
- [x] Переделать toolbar.html: убрать tab bar, оставить только nav bar
- [x] Sidebar: новый `sidebar.html` как отдельный WebContentsView (слева)
- [x] Обновить `layoutViews()`: sidebar (240px) + nav bar (44px) + content
- [x] Добавить IPC для sidebar: collapse/expand, tab management
- [x] Использовать CSS `@layer` для организации стилей
- [x] Добавить `prefers-reduced-motion` для accessibility

---

## 8. Приоритеты

| # | Задача | Приоритет | Сложность | Статус |
|---|--------|-----------|-----------|--------|
| 1 | Исправить баги (extensions, AI keys, find bar) | Критично | Низкая | ✅ Готово |
| 2 | Design tokens + components CSS | Высокий | Средняя | ✅ Готово |
| 3 | Sidebar (новый layout, tab management) | Высокий | Высокая | ✅ Готово |
| 4 | Nav bar (упрощение toolbar) | Высокий | Средняя | ✅ Готово |
| 5 | Glassmorphism (стеклянные эффекты) | Средний | Средняя | ✅ Готово |
| 6 | Редизайн внутренних страниц | Средний | Высокая | ✅ Готово |
| 7 | Анимации (spring, stagger, transitions) | Средний | Средняя | ✅ Готово |
| 8 | newtab редизайн | Низкий | Низкая | ✅ Готово |

---

## Статус: Завершён (основные задачи)

### Выполнено:
- [x] Баг: `extensions.html` — исправлена опечатка `--fg-muоted` → `--fg-muted`
- [x] Design tokens: создан `design-tokens.css` с CSS переменными (glass, radius, transitions, spacing, typography)
- [x] Components CSS: создан `components.css` с переиспользуемыми компонентами
- [x] Sidebar: создан `sidebar.html` с вертикальным списком вкладок, pinned tabs, quick nav
- [x] Nav bar: toolbar.html переделан — убран tab bar, оставлен compact nav bar (44px) с glass URL bar
- [x] Layout: `main.js` обновлён — sidebar (240px) + nav bar (44px) + content
- [x] IPC: добавлен `toggle-sidebar` для сворачивания sidebar
- [x] Preload: добавлен `toggleSidebar` API
- [x] Accessibility: добавлен `prefers-reduced-motion` в design tokens
- [x] Glassmorphism: все внутренние страницы используют glass cards, glass inputs, glass blur эффекты
- [x] newtab.html: убраны часы, glass quick links cards, glass search bar, минимализм
- [x] settings.html: sidebar навигация (General/Privacy/Appearance/Advanced), glass cards, iOS toggles
- [x] bookmarks.html: grid layout с glass cards (favicon + title + URL), glass search
- [x] history.html: timeline layout (вертикальная линия + day dots), группировка по дням, glass items
- [x] downloads.html: glass cards с прогресс-баром, pill-shaped action buttons
- [x] passwords.html: glass unlock screen, glass credential cards, pill buttons
- [x] extensions.html: glass cards с iOS toggle switches, badge статуса
- [x] ai.html: glass chat bubbles, glass input area, glass header, glass sidebar
- [x] github.html: glass item cards для repos/issues/PRs, pill filter buttons, glass profile
- [x] Все страницы подключены к design-tokens.css + components.css (убраны дублированные CSS переменные)
- [x] Анимации: spring easing, stagger fade-in, scale-in для карточек, slide-in для элементов списков
- [x] Баг: Extensions `prompt()` заменён на Electron `dialog.showOpenDialog()` file picker
- [x] Баг: GitHub device flow — теперь получает реальный username через `/user` API после авторизации
- [x] Баг: Private window — используется ephemeral partition (без `persist:`) для изоляции данных
- [x] Баг: AI API ключи — `getAiConfig()` теперь сначала читает из `secret.store`, fallback на localStorage
- [x] Баг: Download toast — теперь кликабельный, открывает скачанный файл
- [x] Стиль: Favicon — все страницы используют `/favicon.ico` с fallback на Google API и letter icon
- [x] Стиль: `border-radius: 0` убран из `common.css` для `button` (конфликтовал с UI)
- [x] Стиль: Плавная анимация переключения dark/light theme через `theme-transition` класс
- [x] Стиль: Единый `font-size: 14px` (--text-md) через base body стили в design-tokens.css
- [x] Техническое: CSS `@layer` declaration добавлен в design-tokens.css
- [x] AI: provider и model сохраняются в secret storage при изменении

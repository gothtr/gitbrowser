# GitBrowser — Полный аудит проекта

> Дата: 13 февраля 2026  
> Версия: 0.2.0  
> Стек: Rust (backend/RPC) + Electron (frontend) + SQLite

---

## 1. КРИТИЧЕСКИЕ БАГИ И УЯЗВИМОСТИ БЕЗОПАСНОСТИ

### 🔴 1.1 Захардкоженные ключи шифрования

**Файлы:** `src/services/github_integration.rs:17-18`, `src/services/ai_assistant.rs:14-15`

GitHub-токены и AI API-ключи шифруются статическими паролями, вшитыми в исходный код:
```rust
const GITHUB_KEY_PASSPHRASE: &str = "gitbrowser-github-key-v1";
const AI_KEY_PASSPHRASE: &str = "gitbrowser-ai-key-v1";
```
Любой, кто имеет доступ к бинарнику или исходникам, может расшифровать все сохранённые токены.

**Исправление:**
- Требовать мастер-пароль перед сохранением секретов
- Использовать fallback-ключ только для миграции старых данных
- Вызывать `rekey_with_master()` при первом вводе мастер-пароля (сейчас вызывается, но только при `password.unlock`)

---

### 🔴 1.2 Сессия сохраняется без шифрования при ошибке

**Файл:** `electron/main.js`, функция `saveSession()`

```javascript
}).catch(() => {
  // Fallback: save unencrypted if encryption fails
  try { fs.writeFileSync(userDataPath('session.json'), jsonStr, 'utf8'); } catch {}
});
```
Если шифрование не удалось (например, RPC-процесс упал), все URL и заголовки вкладок записываются в plaintext.

**Исправление:**
- Убрать fallback на незашифрованное сохранение
- Логировать ошибку, но не сохранять данные в открытом виде

---

### 🔴 1.3 CSP с `unsafe-inline`

**Файл:** `electron/main.js`, `onHeadersReceived`

```javascript
'Content-Security-Policy': ["default-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'..."]
```
`unsafe-inline` полностью обесценивает CSP — XSS-атаки на внутренних страницах возможны.

**Исправление:**
- Убрать `'unsafe-inline'` из `script-src`
- Использовать nonce-based CSP или вынести все inline-скрипты в отдельные файлы
- Для `style-src` допустимо оставить `'unsafe-inline'` (менее критично)

---

### 🔴 1.4 Пароли возвращаются в plaintext через RPC

**Файл:** `src/rpc_server.rs`, метод `password.list`

```rust
let pw = a.password_manager.decrypt_password(c).unwrap_or_default();
json!({"password": pw, ...})
```
Все пароли расшифровываются и отправляются в Electron через stdin/stdout. Если процесс скомпрометирован или логируется stdout — все пароли утекают.

**Исправление:**
- Не возвращать пароли в `password.list` по умолчанию
- Добавить отдельный метод `password.decrypt` для получения одного пароля по ID
- Возвращать пароль только при явном запросе (копирование, автозаполнение)

---

### 🟠 1.5 Нет валидации URL в IPC-обработчике `navigate`

**Файл:** `electron/main.js`

IPC-обработчик `navigate` принимает URL от renderer-процесса без валидации. Потенциально можно загрузить `file://`, `javascript:` или другие опасные схемы.

**Исправление:**
- Валидировать URL перед загрузкой: разрешать только `http://`, `https://`, `gb://`
- Блокировать `file://`, `javascript:`, `data:` схемы

---

### 🟠 1.6 Расширения без песочницы и проверки прав

**Файл:** `src/services/extension_framework.rs`

- Content-скрипты расширений выполняются с полным доступом к DOM страницы
- Permissions из manifest.json парсятся, но нигде не проверяются
- Нет подписи/верификации расширений
- Расширения могут читать файлы с диска через `read_extension_file()`

**Исправление:**
- Реализовать проверку permissions перед инъекцией скриптов
- Добавить whitelist разрешённых API для расширений
- Изолировать content-скрипты в отдельном мире (Electron `worldSafeExecuteJavaScript`)
- Валидировать пути файлов расширений (path traversal)

---

### 🟠 1.7 Инъекция через PowerShell в инсталляторе

**Файл:** `installer/main.js`, функция `create-shortcuts`

```javascript
const ps = `$s=(New-Object -COM WScript.Shell).CreateShortcut('${lnkPath.replace(/'/g, "''")}');...`;
```
Хотя одинарные кавычки экранируются, путь `INSTALL_DIR` берётся из `process.env.LOCALAPPDATA` без санитизации. Специально сформированное имя пользователя Windows может привести к инъекции команд.

**Исправление:**
- Использовать `execFile` с массивом аргументов вместо строковой интерполяции
- Дополнительно санитизировать все пути

---

## 2. БАГИ И НЕДОРАБОТКИ

### 🟡 2.1 `session.save` в RPC пишет в рабочую директорию

**Файл:** `src/rpc_server.rs`, метод `session.save`

```rust
let session_path = "session.json";
std::fs::write(session_path, data).map_err(|e| e.to_string())?;
```
Путь `session.json` относительный — зависит от CWD процесса. В packaged-приложении CWD может быть непредсказуемым.

**Исправление:**
- Использовать абсолютный путь через `app.getPath('userData')` или передавать путь через параметры

---

### 🟡 2.2 `RefCell` в RPC-сервере — не потокобезопасно

**Файл:** `src/rpc_server.rs`

```rust
let app = RefCell::new(App::new("gitbrowser.db").expect("..."));
```
`RefCell` не `Sync` — если в будущем добавить многопоточность (async), будет паника. Сейчас работает, т.к. RPC однопоточный, но это архитектурный долг.

**Исправление:**
- Заменить на `Mutex<App>` или `RwLock<App>` для будущей совместимости

---

### 🟡 2.3 Утечка памяти: `downloads` Map растёт бесконечно

**Файл:** `electron/main.js`

Очистка `downloads` происходит раз в 24 часа, но при активном использовании Map может накопить тысячи записей. `downloadItems` Map тоже не очищается при ошибках.

**Исправление:**
- Ограничить размер Map (например, 500 записей)
- Очищать `downloadItems` при ошибках загрузки

---

### 🟡 2.4 Нет обработки ошибок при инъекции content-скриптов

**Файл:** `electron/main.js`

`executeJavaScript()` для content-скриптов расширений вызывается без обработки ошибок. Сломанный скрипт расширения может нарушить работу страницы.

**Исправление:**
- Оборачивать инъекцию в try/catch
- Логировать ошибки расширений
- Добавить таймаут для скриптов

---

### 🟡 2.5 Приватный режим не очищает данные

**Файл:** `src/services/privacy_engine.rs`

`clear_private_data()` только сбрасывает статистику:
```rust
fn clear_private_data(&mut self) -> Result<(), PrivacyError> {
    self.stats = PrivacyStats::default();
    Ok(())
}
```
Cookies, кэш, localStorage не очищаются.

**Исправление:**
- Реализовать очистку через Electron `session.clearStorageData()`
- Вызывать при закрытии приватного окна

---

### 🟡 2.6 Генерация паролей с modulo bias

**Файл:** `src/services/password_manager.rs`

```rust
random_bytes.iter().map(|b| chars[*b as usize % chars.len()]).collect()
```
`byte % charset_len` даёт неравномерное распределение, если `charset_len` не делит 256.

**Исправление:**
- Использовать rejection sampling: отбрасывать байты >= `256 - (256 % charset_len)`

---

### 🟡 2.7 Нет миграции версий БД

**Файл:** `src/database/migrations.rs`

Все миграции идемпотентны (`CREATE TABLE IF NOT EXISTS`), но нет версионирования. При изменении схемы существующей таблицы (добавление NOT NULL колонки, изменение типа) — нет механизма миграции.

**Исправление:**
- Добавить таблицу `schema_version` с номером версии
- Выполнять миграции последовательно по номерам

---

### 🟡 2.8 `console-message` как IPC-канал — ненадёжно

**Файл:** `electron/main.js`

Контекстные меню в tab-view используют `console.log('__gb_nav_ctx:...')` для передачи действий обратно в main process. Любой сайт может вызвать `console.log` с таким же префиксом.

**Исправление:**
- Использовать `postMessage` + `ipcRenderer` через preload
- Или генерировать уникальный токен для каждого меню

---

## 3. НЕЗАВЕРШЁННЫЕ ФУНКЦИИ

### 3.1 UI страницы

| Страница | Статус | Что не хватает |
|----------|--------|----------------|
| passwords.html | 80% | Нет локализации кнопки "Add", нет enable/disable toggle для расширений |
| extensions.html | 70% | Нет toggle enabled/disabled, нет отображения permissions, нет поиска |
| ai.html | ? | Не проверено — нужен полный аудит |
| github.html | ? | Не проверено — нужен полный аудит |
| settings.html | ? | Не проверено — нужен полный аудит |

### 3.2 Бэкенд

| Функция | Статус | Что не хватает |
|---------|--------|----------------|
| Privacy Engine | 40% | Только список из 13 трекеров, нет реального adblock, нет очистки данных |
| Extension Framework | 50% | Нет песочницы, нет проверки permissions, нет API whitelist |
| AI Assistant | 60% | Бэкенд готов, но нет HTTP-запросов к провайдерам (только хранение ключей) |
| GitHub Integration | 40% | Хранение токена работает, но нет OAuth flow, нет API для repos/issues/PRs |
| Update Manager | 30% | Структура есть, но нет реальной проверки обновлений из Rust |
| Reader Mode | 50% | Извлечение контента работает через JS, Rust-часть — заглушка |

### 3.3 Тесты

| Область | Покрытие | Что не хватает |
|---------|----------|----------------|
| RPC Server | 0% | Нет тестов вообще — критический компонент |
| Extension Framework | 0% | Нет тестов |
| GitHub Integration | 0% | Нет тестов |
| AI Assistant | 0% | Нет тестов |
| Privacy Engine | 0% | Нет тестов |
| Electron (e2e) | 0% | Нет тестов |
| Security | 0% | Нет тестов на неправильный ключ, tampered data, injection |

---

## 4. АРХИТЕКТУРНЫЕ ПРОБЛЕМЫ

### 4.1 Нет tab suspension
Все вкладки держат WebContents в памяти. При 20+ вкладках — значительное потребление RAM.

### 4.2 Нет пагинации для истории/закладок
`history.recent` и `bookmark.list` возвращают все записи. При тысячах записей — тормоза.

### 4.3 Нет rate limiting для RPC
Electron может отправлять неограниченное количество RPC-запросов. DoS через быстрые повторные вызовы.

### 4.4 Нет graceful degradation
Если Rust-процесс упал — все RPC-вызовы падают. Нет UI-индикации, нет fallback.

### 4.5 Дублирование кода
Приватное окно дублирует ~200 строк логики управления вкладками из основного окна. Нужна абстракция.

---

## 5. ПЛАН ИСПРАВЛЕНИЙ (по приоритету)

### Фаза 1: Безопасность (СРОЧНО)

- [ ] **SEC-01**: Убрать захардкоженные ключи шифрования, требовать мастер-пароль
- [ ] **SEC-02**: Убрать fallback на незашифрованное сохранение сессии
- [ ] **SEC-03**: Убрать `unsafe-inline` из CSP, перейти на nonce-based или внешние скрипты
- [ ] **SEC-04**: Не возвращать все пароли в plaintext через `password.list`
- [ ] **SEC-05**: Валидировать URL в IPC-обработчике `navigate`
- [ ] **SEC-06**: Исправить console-message как IPC (заменить на безопасный канал)
- [ ] **SEC-07**: Санитизировать пути в инсталляторе

### Фаза 2: Критические баги

- [ ] **BUG-01**: Исправить путь `session.json` в RPC (абсолютный путь)
- [ ] **BUG-02**: Исправить modulo bias в генерации паролей
- [ ] **BUG-03**: Ограничить размер downloads Map
- [ ] **BUG-04**: Добавить обработку ошибок при инъекции content-скриптов
- [ ] **BUG-05**: Реализовать очистку данных в приватном режиме
- [ ] **BUG-06**: Заменить `RefCell` на `Mutex` в RPC-сервере

### Фаза 3: Тесты

- [ ] **TEST-01**: Unit-тесты для RPC-сервера (все методы)
- [ ] **TEST-02**: Тесты безопасности (wrong key, tampered ciphertext, injection)
- [ ] **TEST-03**: Тесты Extension Framework (install, content script matching)
- [ ] **TEST-04**: Тесты GitHub Integration (store/get token, rekey)
- [ ] **TEST-05**: Тесты AI Assistant (set/get API key, chat history)
- [ ] **TEST-06**: Тесты Privacy Engine (tracker blocking, HTTPS upgrade)

### Фаза 4: Доработки существующих функций

- [ ] **FEAT-01**: Песочница для расширений (permission enforcement, API whitelist)
- [ ] **FEAT-02**: Полноценный Privacy Engine (adblock filter lists, cookie cleanup)
- [ ] **FEAT-03**: Пагинация для history.recent и bookmark.list
- [ ] **FEAT-04**: Tab suspension (выгрузка неактивных вкладок)
- [ ] **FEAT-05**: Версионирование миграций БД
- [ ] **FEAT-06**: Graceful degradation при падении Rust-процесса
- [ ] **FEAT-07**: Рефакторинг: вынести логику вкладок в общий модуль (main + private)

### Фаза 5: Новые функции

- [ ] **NEW-01**: Полноценный OAuth Device Flow для GitHub
- [ ] **NEW-02**: GitHub API: repos, issues, PRs, notifications
- [ ] **NEW-03**: AI Assistant: HTTP-запросы к провайдерам, streaming ответов
- [ ] **NEW-04**: Автозаполнение паролей на веб-страницах (улучшить текущую реализацию)
- [ ] **NEW-05**: Синхронизация закладок/паролей через GitHub Gists
- [ ] **NEW-06**: Импорт закладок/паролей из Chrome/Firefox
- [ ] **NEW-07**: Группировка истории по дням
- [ ] **NEW-08**: Профили пользователей
- [ ] **NEW-09**: Поддержка дополнительных локалей (uk, de, es, zh)
- [ ] **NEW-10**: Система уведомлений (GitHub, загрузки, обновления)
- [ ] **NEW-11**: Менеджер сертификатов
- [ ] **NEW-12**: Встроенный DevTools toggle для разработчиков

---

## 6. РЕЗЮМЕ

**Сильные стороны:**
- Хорошая архитектура Rust + Electron с чистым RPC-протоколом
- Качественная криптография (AES-256-GCM, PBKDF2 100k итераций, zeroize)
- Модульная структура с trait-based абстракциями
- Хорошее покрытие тестами для core-компонентов

**Главные риски:**
- Захардкоженные ключи шифрования — критическая уязвимость
- CSP с `unsafe-inline` — XSS на внутренних страницах
- Пароли в plaintext через RPC — утечка при компрометации процесса
- Расширения без песочницы — произвольное выполнение кода

**Оценка готовности:** ~55% до production-ready. Безопасность требует немедленного внимания.
